// API Configuration
// Replace placeholders with actual values when the backend is ready.

export const ApiConfig = {
  baseUrl: 'ADD API ENDPOINT HERE',
  timeout: 10000,
  useMock: true, // Set to false when production API is available
};
